# NanoClaw 记忆系统演进分析

本文基于 NanoClaw 官方文档与源码，详细分析其记忆系统从原始设计到当前实现的演进过程。

---

## 一、原始设计：文件即记忆（File-as-Memory）

根据 NanoClaw 官方文档（`docs/REQUIREMENTS.md`），原始记忆系统的核心设计理念是**极简主义**和**文件中心式**存储。

### 1.1 核心机制

**Per-group CLAUDE.md**：
- 每个群组拥有独立的 `CLAUDE.md` 文件
- 位于 `groups/{name}/CLAUDE.md`
- 容器启动时完整加载到系统提示词

**Global CLAUDE.md**：
- 根目录 `groups/CLAUDE.md` 可被所有群组读取
- 但只有 "main" 频道（自我聊天）有权写入
- 用于存储跨群组的通用知识

**SQLite 消息库**：
- 仅存储原始消息记录（`chat_jid`, `sender_jid`, `content`, `timestamp`）
- 无任何语义提取或向量化处理
- 查看 `src/db.ts` 中的实现：

```typescript
export function storeMessage(msg: NewMessage): void {
  const sql = `
    INSERT INTO messages (chat_jid, sender_jid, content, timestamp, is_from_me)
    VALUES (?, ?, ?, ?, ?)
  `;
  db.prepare(sql).run(
    msg.chatJid,
    msg.senderJid,
    msg.content,
    msg.timestamp,
    msg.isFromMe ? 1 : 0
  );
}
```

### 1.2 原始设计的局限性

官方文档中明确提到 Session Management 的策略：

> "Sessions auto-compact when context gets too long, preserving critical information"

但这存在根本性问题：

1. **无自动提取**：对话结束后，有价值的信息不会自动转化为长期记忆
2. **人工维护负担**：用户必须手动编辑 `CLAUDE.md` 来更新知识
3. **检索效率低**：历史消息堆积后，缺乏有效的检索机制
4. **无跨群组共享**：知识孤岛效应，经验无法沉淀复用

---

## 二、当前实现：向量记忆系统（Vector-based Memory）

基于本地源码分析（`~/git/Agents/NanoClaw`），记忆系统已升级为**主动提取 + 向量存储**架构。

### 2.1 新增核心组件

查看 `src/memory/` 目录结构：

```
src/memory/
├── types.ts          # 记忆类型定义
├── extractor.ts      # 记忆提取器
├── trigger.ts        # 触发逻辑
├── store.ts          # 存储层
├── deduplicator.ts   # 去重合并
├── hierarchical.ts   # 分层存储
├── local-embedding.ts # 本地向量生成
├── server.ts         # MCP 服务端
└── proactive.ts      # 主动预测
```

### 2.2 关键技术实现

**（1）记忆类型体系**（`src/memory/types.ts`）：

```typescript
export type MemoryType = 'fact' | 'preference' | 'skill' | 'goal' | 'context';

export interface Memory {
  id: string;
  groupFolder: string;
  type: MemoryType;
  content: string;
  embedding: number[] | null;  // 向量表示
  sourceType: 'extraction' | 'manual' | 'proactive';
  createdAt: string;
  updatedAt: string;
}
```

**（2）双轨触发机制**（`src/index.ts` 新增代码）：

```typescript
import { triggerMemoryExtraction } from './memory/trigger.js';
import { MEMORY_TRIGGER_INTERVAL_MINUTES } from './config.js';

// 1. 定时触发（每 10 分钟）
if (MEMORY_TRIGGER_INTERVAL_MINUTES > 0) {
  const intervalMs = MEMORY_TRIGGER_INTERVAL_MINUTES * 60 * 1000;
  setInterval(async () => {
    const groups = getAllRegisteredGroups();
    for (const [jid, group] of Object.entries(groups)) {
      await triggerMemoryExtraction(group.folder, sinceTimestamp);
    }
  }, intervalMs);
}

// 2. 事件触发（Agent 容器完成后）
// 位于 src/ipc.ts，当收到 _close 信号时触发
if (data.signal === '_close') {
  await triggerMemoryExtraction(sourceGroup, sinceTimestamp);
}
```

**（3）分层存储设计**（`src/memory/hierarchical.ts`）：

引入 L0/L1/L2 三级抽象：
- **L0（极简摘要）**：10-20 词的核心信息，用于向量检索
- **L1（结构化概要）**：快速全景铺垫
- **L2（完整叙事）**：详细上下文，按需加载

**（4）去重合并逻辑**（`src/memory/deduplicator.ts`）：

```typescript
export interface DeduplicationResult {
  decision: 'CREATE' | 'MERGE' | 'SKIP';
  existingMemoryId?: string;
  similarityScore: number;
}

// 流程：
// 1. 向量相似度预筛选（余弦相似度 > 0.85）
// 2. LLM 决策：创建新记忆 / 合并现有记忆 / 跳过重复
```

### 2.3 配置体系更新

查看 `src/config.ts` 新增配置：

```typescript
// Memory system configuration
export const MEMORY_DIR = path.join(NANOCLAW_HOME, 'memory');
export const MEMORY_EMBEDDING_MODEL = process.env.MEMORY_EMBEDDING_MODEL || 'text-embedding';
export const MEMORY_EXTRACTION_MODEL = process.env.MEMORY_EXTRACTION_MODEL || 'glm-4';
export const MEMORY_TRIGGER_INTERVAL_MINUTES = parseInt(
  process.env.MEMORY_TRIGGER_INTERVAL || '10',
  10
);
```

---

## 三、演进对比：V1 vs V2

| 维度 | V1 原始设计 | V2 当前实现 |
|------|------------|------------|
| **存储介质** | Markdown 文件 + SQLite 原始消息 | SQLite + 向量索引 + 分层 Markdown |
| **记忆生成** | 人工编辑 CLAUDE.md | 自动提取对话中的有价值信息 |
| **触发机制** | 无（纯被动） | 定时触发 + Agent 完成事件触发 |
| **检索方式** | 关键词匹配（无实现） | 向量相似度检索 |
| **去重策略** | 无 | 向量预筛 + LLM 决策合并 |
| **分层存储** | 无（单层文件） | L0/L1/L2 三级抽象 |
| **跨群组共享** | 仅 Global CLAUDE.md（人工维护） | 规划中：脱敏后全局共享 |
| **遗忘机制** | 无（文件单向增长） | 规划中：热度衰减算法 |

---

## 四、为什么需要这些更新？

### 4.1 解决记忆不会自动生长的问题

**V1 的问题**：
- 用户必须手动整理和编辑 `CLAUDE.md`
- 对话中产生的知识、偏好、决策无法自动沉淀
- AI 永远停留在上次手动编辑时的状态

**V2 的解决方案**：
- 引入记忆提取器，自动识别对话中的事实型信息
- 双轨触发机制确保无遗漏：定时检查 + 事件触发
- 用户无需手动维护，记忆随对话自动积累

### 4.2 解决检索效率低下的问题

**V1 的问题**：
- 历史消息堆积后，Agent 只能看到最近的几轮对话
- 缺乏语义理解，无法处理模糊查询

**V2 的解决方案**：
- 使用 Embedding 技术将文本转化为向量
- 基于向量相似度进行语义检索
- 分层存储：L0 用于快速匹配，L2 用于深度参考

### 4.3 解决上下文膨胀与成本失控

**V1 的问题**：
- `CLAUDE.md` 随时间单向增长
- 每次对话都加载完整文件，Token 成本线性上升
- 有效信息密度随文件增大而下降

**V2 的解决方案**：
- 去重合并机制防止冗余记忆堆积
- 分层存储：日常对话只加载 L0 摘要
- 规划中：遗忘机制自动清理低频记忆

### 4.4 解决知识孤岛效应

**V1 的问题**：
- 每个群组的 `CLAUDE.md` 完全隔离
- 通用经验需要在多个群组重复声明
- 群体智慧无法涌现

**V2 的解决方案**：
- 规划中：IPC 语义总线实现受控共享
- 局部记忆 vs 全局记忆的严格分级
- 脱敏后高阶模式可跨群组复用

---

## 五、技术选型的工程考量

### 5.1 为什么选择 Embedding + 向量检索？

相比传统关键词搜索，Embedding 的核心优势：

1. **语义理解**：理解"我喜欢 Python"和"我偏好 Python 而非 JavaScript"的等价性
2. **模糊匹配**：支持"上次说的那个优化方案"这类非精确查询
3. **关联发现**：自动发现语义相近但表述不同的记忆

### 5.2 为什么选择本地 SQLite 而非专用向量数据库？

根据 NanoClaw 的"极简主义"哲学：

1. **最小化依赖**：避免引入 Pinecone、Milvus 等外部服务
2. **数据主权**：所有数据保留在本地，无云服务依赖
3. **架构一致性**：复用现有的 SQLite 基础设施
4. **规模适配**：对于个人/小团队，纯 JavaScript 向量运算已足够

### 5.3 双轨触发策略的设计 rationale

**定时触发（每 10 分钟）**：
- 捕获普通聊天中的有价值信息
- 无需等待 Agent 任务完成

**事件触发（Agent 完成后）**：
- 在复杂任务执行后立即提取经验
- 确保技术决策和问题解决记录被及时保存

这种设计平衡了实时性与成本：既不会频繁调用 API，也不会遗漏重要信息。

---

## 六、未完成的规划与未来方向

当前实现（截至 2026-03-01）已完成：
- ✅ 记忆提取管线
- ✅ 向量存储与检索
- ✅ 去重合并逻辑
- ✅ 双轨触发机制

仍在规划中：
- ⏳ 智能上下文装配（只加载相关记忆到提示词）
- ⏳ 仿生遗忘机制（热度衰减算法）
- ⏳ 记忆投影（定期生成可读 Markdown 摘要）
- ⏳ 跨群组知识广播（经审核的通用模式共享）
- ⏳ 高阶模式自举（从经验自动提炼技能草稿）

---

## 七、总结

NanoClaw 记忆系统的演进，体现了从**人工维护**到**自动提取**、从**语法匹配**到**语义检索**、从**单层存储**到**分层抽象**的认知架构升级。

核心洞察：
1. **自动优于手动**：记忆系统必须主动提取，不能依赖用户维护
2. **语义优于语法**：向量检索比关键词匹配更接近人类记忆的联想方式
3. **分层优于扁平**：不同场景需要不同粒度的记忆表示
4. **遗忘优于堆砌**：记忆的精髓在于筛选，而非无差别存储

这一演进并未违背 NanoClaw "小即是美"的初心——所有新增逻辑仍保持轻量级，通过解耦为独立模块（`src/memory/*`）实现可理解性与可维护性。
